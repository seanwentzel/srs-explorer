{
    "files": [
        "src/"
    ],
    "scripts": {
        "start": "rm -r dist && parcel serve src/index.html",
        "build": "parcel build src/index.html --public-url .",
        "deploy": "parcel build src/index.html --public-url . && gh-pages -d dist"
    },
    "devDependencies": {
        "gh-pages": "^6.0.0",
        "parcel": "^2.10.0",
        "typescript": "^5.2.2"
    }
}
